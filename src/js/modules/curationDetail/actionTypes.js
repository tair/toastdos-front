"use strict";

import { name } from './constants';

export const ATTEMPT_SUBMIT = `${name}/ATTEMPT_SUBMIT`;
export const SUBMIT_SUCCESS = `${name}/SUBMIT_SUCCESS`;
export const SUBMIT_FAIL = `${name}/SUBMIT_FAIL`;
export const RESET_SUBMISSION = `${name}/RESET_SUBMISSION`;
export const PREVIEW = `${name}/PREVIEW`;
export const EDIT = `${name}/EDIT`;
export const LOAD_SUBMISSION = `${name}/LOAD_SUBMISSION`;

export const SET_PUBLICATION = `${name}/SET_PUBLICATION`;
export const ADD_GENE = `${name}/ADD_GENE`;
export const REMOVE_GENE = `${name}/REMOVE_GENE`;
export const ADD_ANNOTATION = `${name}/ADD_ANNOTATION`;
export const REMOVE_ANNOTATION = `${name}/REMOVE_ANNOTATION`;

export const REQUEST_SUBMISSION = `${name}/REQUEST_SUBMISSION`;
export const FAIL_SUBMISSION = `${name}/FAIL_SUBMISSION`;