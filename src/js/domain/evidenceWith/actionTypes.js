"use strict";

import { name } from './constants';

export const ADD = `${name}/ADD`;
export const REMOVE = `${name}/REMOVE`;
export const REMOVE_MULTIPLE = `${name}/REMOVE_MULTIPLE`;
export const UPDATE = `${name}/UPDATE`;
export const CLEAR = `${name}/CLEAR`;
export const ATTEMPT_VALIDATE = `${name}/ATTEMPT_VALIDATE`;
export const VALIDATE_SUCCESS = `${name}/VALIDATE_SUCCESS`;
export const VALIDATE_FAIL = `${name}/VALIDATE_FAIL`;
